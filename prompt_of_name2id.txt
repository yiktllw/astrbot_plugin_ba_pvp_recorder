这是蔚蓝档案名称映射任务（第二步：名称补 id）。

输入会提供：
1. 未命中名称列表
2. 第一阶段识别结果（JSON）
3. 学生映射 JSON

请你只输出 JSON，不要输出解释，格式必须为：
{
  "mappings": [
    {"name": "原名称", "id": "学生id或空字符串"}
  ]
}

要求：
1. 只做名称到 id 的映射，不要改写原名称。
2. 保留简体/繁体/英文原样用于 name 字段。
3. 无法确定时 id 返回空字符串。
4. 不要输出 markdown 代码块。
5. 第一阶段使用视觉识别，若没有完全匹配的名称则优先考虑字形相近的结果。

未命中名称列表：
{{UNRESOLVED_NAMES}}

第一阶段识别结果：
{{CURRENT_BATTLES}}

学生映射 JSON：
{{STUDENTS_JSON}}
